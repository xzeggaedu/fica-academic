# Progreso del Proyecto: fica-academic

## Cambios realizados al 21-Agosto-2025

- Se configuró el frontend (React + Vite) dentro del proyecto y se agregó al `docker-compose.yml` para levantarlo junto al backend.
- Se creó el `Dockerfile` para el frontend, permitiendo modo desarrollo con hot reload usando volúmenes y el comando `npm run dev`.
- Se ajustó el backend en `docker-compose.yml` para usar `uvicorn --reload`, permitiendo hot reload y desarrollo ágil.
- Se corrigió el mapeo de puertos para el frontend, exponiendo correctamente el puerto 5173 en localhost:3000.
- Se resolvieron conflictos de nombres de contenedores en Docker eliminando la opción `container_name` en la base de datos.
- Se explicaron y configuraron comandos para linters, entornos virtuales y cómo ignorar validaciones en los commits.
- Se documentó el flujo para desarrollo ágil con Docker y hot reload en ambos servicios.

---
